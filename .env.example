# Copy to .env and fill in real values. Never commit .env itself.
FINNHUB_API_KEY=your_finnhub_key_here

# Optional — /api/chart returns [] without it (see D4). Everything else works.
POLYGON_API_KEY=

# CORS origin for the backend. Change per environment when we hit the cloud.
FRONTEND_ORIGIN=http://localhost:8501